# docker build 시 엔진으로 보내지 않을 것들.

# 수백 MB. 컨테이너 안에서 npm ci 로 새로 설치하므로 보낼 필요가 없다.
# (호스트가 Windows라 여기서 만들어진 node_modules는 리눅스에서 못 쓰기도 한다)
node_modules/

# 컨테이너 안에서 npm run build 로 새로 만든다.
dist/

.git/
*.md
*.log
